defmodule Cohort.Sample.API do
  def join_room(name, user) do
    # with \
    #   {:ok, cohort} <- Cohort.Pool.join({:room, name}),
    #   {:ok, service} <- Cohort.Service.spawn(cohort, {Cohort.Sample.Service, user})
    # do

    # end
  end
end